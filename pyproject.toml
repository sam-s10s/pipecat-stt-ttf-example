[project]
name = "stt-timing"
version = "0.1.0"
description = "wip"
requires-python = ">=3.12"
dependencies = [
    "pipecat-ai[webrtc,deepgram,speechmatics,silero,runner]>=0.0.84",
    ]

[dependency-groups]
dev = [
    "ruff~=0.12.1",
]

[tool.ruff]
line-length = 100
[tool.ruff.lint]
select = ["I"]